# 非法委派请求的恢复

实际任务给 evidence_only 子节点配置了 search_mcp_tools、use_mcp_tool 和
agent.read_tool_result。仅证据模式禁止工具，原权限校验正确，但恢复路径
直接传播解析/语义错误，主运行反复失败且没有工具失败结果可供模型纠正。

首次请求和恢复请求现在复用相同的委派解析及图校验。请求非法时，在启动
子图前记录普通 ToolFinished 失败并交还主模型。有效请求仍经过执行时校验，
不会通过自动切换隔离模式、删除能力或扩大授权来使请求通过。

工具说明与错误信息明确：evidence_only 使用 capabilities: []；
read_only_project 仅允许冻结的只读能力。额外网络检索由有授权的主 Agent
执行，不能将 use_mcp_tool 交给仅证据子节点。

确定性恢复测试分别播种缺少 nodes 的请求和 evidence_only 携带工具的请求，
断言它们返回失败结果、没有启动非法子图，之后合法子图运行且主任务完成。
真实模型/SSH 科研任务端到端验收未执行。

本次检查：Rust workspace 424 通过、11 ignored；前端 151 和扩展 22 个测试
通过；Web 构建通过。测试辅助模型改为引用当前成功工具事件的实际序号，避免
恢复时插入失败事件造成测试证据引用偏移；委派相关 14 个测试再次通过。
